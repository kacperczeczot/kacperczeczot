---
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import Lightbox from '../components/Lightbox.astro';
import EnsembleDrawer from '../components/EnsembleDrawer.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const base = import.meta.env.BASE_URL.replace(/\/$/, '');

const {
  title = 'Kacper Czeczot — Dyrygent, Muzyk & Reżyser Dźwięku',
  description = 'Oficjalne portfolio artystyczne Kacpra Czeczota: dyrygentura symfoniczna i chóralna, wykonawstwo sceniczne, autorskie produkcje muzyczne oraz warsztat twórczy.',
  image = `${base}/images/og-cover.svg`,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site || 'https://kacperczeczot.github.io');
---

<!doctype html>
<html lang="pl" class="scroll-smooth bg-[#070d1a] text-slate-100 antialiased">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonicalURL} />

    <!-- SEO & Metadata -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Kacper Czeczot" />
    <meta name="theme-color" content="#070d1a" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />

    <!-- Twitter Card -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={image} />

    <!-- Google Fonts: Plus Jakarta Sans (Body) & Outfit (Headings) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="min-h-screen flex flex-col bg-[#070d1a] text-slate-100 selection:bg-sky-500/30 selection:text-sky-200 relative overflow-x-hidden">
    
    <!-- Atmospheric Oceanic Blue Background Lights -->
    <div class="fixed top-0 left-1/2 -translate-x-1/2 w-full max-w-7xl h-[600px] bg-gradient-radial from-sky-500/10 via-blue-700/5 to-transparent pointer-events-none -z-10 blur-3xl"></div>
    <div class="fixed -top-40 right-1/4 w-[500px] h-[500px] bg-sky-400/8 rounded-full blur-[140px] pointer-events-none -z-10"></div>
    <div class="fixed top-1/2 -left-40 w-[600px] h-[600px] bg-blue-600/8 rounded-full blur-[160px] pointer-events-none -z-10"></div>

    <!-- Navigation -->
    <Navbar />

    <!-- Main Content Container -->
    <main class="flex-1 w-full relative">
      <slot />
    </main>

    <!-- Footer -->
    <Footer />

    <!-- Global Components -->
    <EnsembleDrawer />
    <Lightbox />
  </body>
</html>
